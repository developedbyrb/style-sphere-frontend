import { Routes } from "@angular/router";
import { UpsertShopComponent } from "./upsert-shop.component";

export const routes: Routes = [
    {
        path: '',
        component: UpsertShopComponent
    }
]